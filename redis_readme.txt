Redis for Windows
https://github.com/MicrosoftArchive/redis/releases

download https://github.com/MicrosoftArchive/redis/releases/download/win-3.2.100/Redis-x64-3.2.100.msi
it installs redis default, sets it up as a running service, works out of the box.
on installing remain the default settings

server should run on localhost and port 6379

Redis for Linux
https://redis.io/download

newest version, make sure it runs on localhost and port 6379, but this should be by default